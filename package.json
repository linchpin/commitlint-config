{
  "name": "@linchpin/commitlint-config",
  "version": "1.0.0",
  "description": "A standardized commitlint configuration for Linchpin projects",
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "scripts": {
    "test": "jest"
  },
  "keywords": [
    "commitlint",
    "commitlint-config",
    "conventional-commits"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@commitlint/cli": ">=17.0.0",
    "@commitlint/config-conventional": ">=17.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "jest": "^29.0.0"
  }
}
